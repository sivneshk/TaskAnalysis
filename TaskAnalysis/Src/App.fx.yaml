App As appinfo:
    BackEnabled: =false
    OnStart: |
        =/*Set Global Variable */
        Set(iterator, 0);                  /* this is for Color ID in TaxonomyCollect */               
        Set(varEdit, DisplayMode.View);                 /* so the Manager Review Screen is in View Mode upon OnStart */
        Set(inProgress, DisplayMode.Edit);
        
        /* Clear all non-permanent Collections OnStart */
        Clear(colTaxonomy);
        Clear(colTaxonomyTemp);
        Clear(FinalTaxonomy);
        Clear(colToDelete);
        
        
        ClearCollect(colTaskAnalysisSP, 'Task Analysis Taxonomy Collection');
        
        ClearCollect(
            PrepCollection,
            ForAll(
                Table(ParseJSON(Task_A_PreparerHours.Run("ahmedelsaidmohamed.baioumy@bakerhughes.com").json).body),
                {
                    Email: Text(Value.Email),
                    Coco: Text(Value.Coco),
                    PrepHours: Text(Value.PrepHours)
                }
            )
        );
        
        ClearCollect(
            AppCollection,
            ForAll(
                Table(ParseJSON(Task_A_ApproverHours.Run("bassem.abdelrahman@bakerhughes.com").json).body),
                {
                    Email: Text(Value.Email),
                    Coco: Text(Value.Coco),
                    AppHours: Text(Value.AppHours)
                }
            )
        ); 
        
        /* ClearCollect(
            PrepCollection,
            ForAll(
                Table(ParseJSON(Task_A_PreparerHours.Run("Office365Users.MyProfile().Mail").json).body),
                {
                    Email: Text(Value.Email),
                    Coco: Text(Value.Coco),
                    PrepHours: Text(Value.PrepHours)
                }
            )
        );
        
        ClearCollect(
            AppCollection,
            ForAll(
                Table(ParseJSON(Task_A_ApproverHours.Run("Office365Users.MyProfile().Mail").json).body),
                {
                    Email: Text(Value.Email),
                    Coco: Text(Value.Coco),
                    AppHours: Text(Value.AppHours)
                }
            )
        );*/
    StartScreen: =Switch(Param("ScreenName"), "MainPage", TaskAnalysisScreen, "asManager", 'ForManagerEditScreen/UserHomeScreen')
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

